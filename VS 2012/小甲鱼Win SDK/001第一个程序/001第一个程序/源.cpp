#include <windows.h>


int WINAPI WinMain (HINSTANCE hInstance ,HINSTANCE hprevInstance ,PSTR szCmdLine ,int iCmdShow)
{
	//0
	//MessageBox(NULL, TEXT("这是我在鱼C写的第一个Windows程序！"),TEXT("打招呼"), MB_ABORTRETRYIGNORE );

	//1
	//MessageBox(NULL, TEXT("你真的忍心把我关掉吗？"),TEXT("鱼C工作室"), MB_YESNO );

	//2
	/*int choose = MessageBox(NULL, TEXT("你真的忍心把我关掉吗？"),TEXT("鱼C工作室"), MB_YESNO );

	while (choose != IDYES)
	{
	choose = MessageBox(NULL, TEXT("你真的忍心把我关掉吗？"),TEXT("鱼C工作室"), MB_YESNO );
	}*/

	return 0;
}